package config

const (
    // AppName defines the application binary name
    AppName = "dhivesd"
    
    // Bech32Prefix defines the Bech32 prefix used for EthAccounts
    Bech32Prefix = "dhives"

    // Bech32PrefixAccAddr defines the Bech32 prefix of an account's address
    Bech32PrefixAccAddr = Bech32Prefix
    
    // Denom defines the coin denomination used in Dhives
    Denom = "dhives"
    
    // BaseDenom defines the base denomination used in Dhives
    BaseDenom = "dhives"
    
    // DisplayDenom defines the denomination shown to users in client applications
    DisplayDenom = "DHIVES"
)
